@model Timesheet.Models.ViewModels.ProjectViewModel

@{
    ViewData["Title"] = "Create Project";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create a new Project</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DepartmentOwnerId" class="control-label">Department</label>
                @*<input asp-for="DepartmentOwnerId" class="form-control" />*@
                <div>
                    @Html.DropDownListFor(m => m.DepartmentOwnerId, ViewBag.Departments as IEnumerable<SelectListItem>, "Select a department")
                </div>
                <span asp-validation-for="DepartmentOwnerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}